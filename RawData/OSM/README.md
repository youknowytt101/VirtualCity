# OSM 数据

> 存放从 OpenStreetMap / Overpass Turbo 导出的 `.osm` 文件。

---

## 推荐命名

```text
{area_id}_osm_v001.osm
```

示例：

```text
mvp_area_osm_v001.osm
```

---

## 记录要求

每个 `.osm` 文件必须在 `项目管理/区域记录/{area_id}.md` 中记录：

- bbox。
- 下载时间。
- 数据来源。
- 原始坐标系。
- 是否经过二次处理。

---

## 当前状态

尚未放入 MVP OSM 数据。
