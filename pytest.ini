[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = Scripts
markers =
    e2e: real-browser tests (Playwright) that start an isolated server + Chromium; slower than the rest of the suite. Skip with -m "not e2e" for a fast loop.
