# 02 当前状态与下一步

> 本文件是项目状态面板。  
> 每次阶段性工作完成后，应更新本文件，让下一次 AI 打开项目时能直接接上。  
> **最后更新：2026-05-22（Session 13）**

---

## 1. 当前阶段

当前阶段：**跨机器稳定化 + Houdini 白盒质量提升阶段**。

最新完成（Session 13）：

- 本地已同步到远端最新 `43c9c54 Session 12b`。
- 新增 `自动化插件/vc_paths.py`，所有核心自动化脚本从脚本位置自动推导项目根目录，不再依赖 `F:/VirtualCity` 或 `D:/VirtualCity`。
- `配置/active_area.json` 改为相对路径，可在家里/公司/新机器复用。
- 修复 `validate_data.py` 在 Windows GBK 控制台输出 emoji 导致 `UnicodeEncodeError` 的问题。
- 修复 UE5 `Content/Python/init_vc.py` 触发文件路径硬编码，改为从当前 UE5 工程向上查找 VirtualCity 根目录。
- 静态验证通过：Python AST 全部通过，`validate_data.py` 在当前 `d:\VirtualCity` 下数据预检通过。

当前缺口：
- Houdini 当前未连接，本轮未做视口级验证。
- `Export + UE5` 仍需结合 UE5 编辑器做实际导入验证。
- Copernicus 10m DEM 403，回退 NASADEM 30m（精度较低）
- Houdini 白盒下一步：建筑 foundation/skirt、道路交叉口清理、水体/landuse 白盒。

---

## 1.5 当前里程碑（2026-05-21 更新）

| 阶段 | 状态 | 说明 |
|---|---|---|
| 区域 bbox 确定 | ✅ | pattaya_sai6_mvp，west=100.866 / south=12.922 / east=100.882 / north=12.938 |
| OSM 数据下载 | ✅ | `原始数据/OSM/pattaya_sai6_mvp_osm_v001.osm`（1,268 KB）|
| Overture 数据下载 | ✅ | 1,226 栋建筑，含 subtype，待切换为主数据源 |
| Houdini 节点网络 | ✅ | 773 建筑（拉伸 + 程序化高度）+ 21,437 道路面，共 32,208 面 |
| 建筑数据源切换 Google Open Buildings 2.5D | ✅ | 2,046 栋，100% 有真实高度（1.9m~59.3m），自动化脚本已实现 |
| FBX 分组导出 | ✅ | buildings_v001.fbx + roads_v001.fbx + terrain_v001.fbx，`convertunits=1` |
| UE5 项目创建与导入 | ✅ | VirtualCityUE.uproject，无头导入 + Remote Control 自动放置 |
| 跨机器路径稳定化 | ✅ | `vc_paths.py` + 相对 `active_area.json` + UE5 trigger 自动定位 |
| UE5 Bake 验证 | ⏸ 暂缓 | MVP 流程已验证，Bake 留待展示阶段 |

---

## 2. 当前主目标

完成第一个 `200m × 200m` 到 `500m × 500m` 的城市 MVP 区块。

MVP 需要验证：

```text
OSM 数据
    ↓
Houdini 导入和白盒生成
    ↓
HDA 输出
    ↓
UE5 导入和 Bake
    ↓
基础展示关卡
```

---

## 3. 当前推荐下一步（接手直接从这里开始）

### Step 1（已完成）：切换建筑数据源为 Google Open Buildings 2.5D

**结果**：2,046 栋建筑，100% 有真实高度（1.9m ~ 59.3m），Houdini `osm_import` SOP 已更新。

**自动化**：后续更新数据只需双击 `自动化插件/run_download_heights.bat`

### Step 2（已完成）：分组导出 FBX

 buildings_v001.fbx + roads_v001.fbx 已导出到 `Houdini/Export/`

### Step 3（已完成）：创建 UE5 项目并导入验证

- `UE5/VirtualCityUE/VirtualCityUE.uproject` 已创建
- 无头导入 + Remote Control API 自动放置到场景，白盒城市可视化验证通过

### ✅ MVP 第一轮管线完成

**完整链路**：GEE 建筑高度 → Houdini 程序化生成 → FBX 导出 → UE5 导入 → 场景展示

### 下一阶段方向（Session 8 后）

**P0 首选**：
- 在 Houdini 打开当前 hip，运行 `uv run python _recook_new_area.py` 做视口级验证。
- 运行 `uv run python export_and_import.py`，确认 UE5 触发导入在当前机器路径下正常。

**P1 可选**：
- 建筑 foundation/skirt，解决坡地下坡侧悬空。
- 道路交叉口清理，减少条带重叠、缝隙和尖角。
- 水体/landuse 白盒分组输出。

---

## 4. 当前文件状态

| 类型 | 路径 | 状态 |
|---|---|---|
| Houdini Hip | `Houdini/Hip/VC_pattaya_sai6_mvp_citygen_v001.hip` | ✅ 已保存（真实高度建筑 + 道路 + 地形）|
| OSM 数据 | `原始数据/OSM/pattaya_sai6_mvp_osm_v001.osm` | ✅ |
| Overture 建筑 GeoJSON | `原始数据/Overture/pattaya_sai6_mvp_buildings_v001.geojson` | ✅ 已下载（1,226 栋）|
| FBX 导出 | `Houdini/Export/` | ✅ buildings/roads/terrain（convertunits=1）|
| UE5 工程 | `UE5/VirtualCityUE/` | ✅ 三个 Mesh 已导入，触发文件自动化已部署 |
| 路径工具 | `自动化插件/vc_paths.py` | ✅ 跨机器路径统一入口 |
| 建筑高度下载脚本 | `自动化插件/download_building_heights.py` | ✅ 一键下载，无需 Drive |
| 坑点记录 | `项目管理/12_已知坑点与解决方案.md` | ✅ H-001~H-008 + O-001~O-002 + U-001~U-002 |
| 任务看板 | `项目管理/08_任务看板.md` | ✅ |
| 迭代日志 | `项目管理/03_迭代日志.md` | ✅ |

---

## 5. 当前风险

| 风险 | 处理方式 |
|---|---|
| 过早做大地图 | 严格先做 200m～500m MVP |
| 过早开发自动化工具 | 重复 3 次以上再脚本化 |
| OSM 高度数据缺失 | Houdini 中用默认楼层和随机规则补全 |
| 国内地图坐标偏移 | 先明确数据源坐标系，必要时再做转换 |
| UE5 / Houdini Engine 版本不匹配 | 以 Houdini Engine 支持版本为准选择 UE 版本 |
| 跨机器盘符不一致 | 所有脚本必须经 `vc_paths.py` 推导根目录，禁止硬编码 `F:/VirtualCity` / `D:/VirtualCity` |

---

## 6. 下一次 AI 接手提示

**必读顺序**：本文件 → `项目管理/12_已知坑点与解决方案.md` → `项目管理/08_任务看板.md`

**当前任务**：Houdini 视口级验证 + UE5 导出导入验证；随后推进建筑 foundation/skirt 和道路交叉口清理。

**一键迭代命令**：在 `自动化插件/` 下运行 `uv run python _recook_new_area.py`，确认视口正常后运行 `uv run python export_and_import.py`。

**AI 自动迭代规范**（用户要求，每次工作完成后执行）：
1. 更新本文件（里程碑表 + 下一步）
2. 追加 `03_迭代日志.md` 条目
3. 更新 `08_任务看板.md`（进行中 / 已完成）
4. 如有新坑点，追加 `12_已知坑点与解决方案.md`
5. 更新 AI 持久记忆（重大决策/数据源变更）
6. `git add .; git commit -m "..."` 提交

不需要用户提醒，AI 应主动完成以上步骤。
