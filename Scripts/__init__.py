"""VirtualCity automation package.

The scripts in this package are still exposed as CLI entry points for the
current workflow, but shared code should prefer normal package imports over
ad-hoc path injection as the project continues to split into layers.
"""

